FleetPilot V18.8 — Handover Commit Verification Fix

Исправлено:
- После нажатия «Подтвердить приём» форма больше не должна возвращаться в состояние ожидания при реально сохранённой выдаче.
- Расширено распознавание успешной приёмки: поддерживаются ответы RPC с status=issued/active/accepted и issue_at, даже без отдельного accepted_at.
- Поддерживаются фото как issue_photos, а не только issue_photos_count.
- После submit клиент повторно проверяет get_driver_handover_state и историю передач.
- Если сервер действительно не сохранил приёмку, водитель увидит явную ошибку вместо бесконечного цикла формы.
- При успешном подтверждении локальный Driver Portal сразу переключается в состояние «Автомобиль принят».

Заменить:
- index.html
- sw.js
- fp-driver-portal.js
