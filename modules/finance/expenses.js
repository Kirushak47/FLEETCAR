/* FleetPilot 20 — Finance expense domain */
(()=>{'use strict';const FP=window.FleetPilot=window.FleetPilot||{},Core=FP.Core;if(!Core||FP.FinanceExpenses)return;const same=(a,b)=>String(a??'')===String(b??'');function rows(bounds,carId=null){const d=Core.db();if(!d)return{paidExpenses:[],legacyRepairs:[]};normalizeRepairExpenseLinks?.();const paidExpenses=(d.expenses||[]).filter(x=>(!carId||same(x.carId,carId))&&x.status==='paid'&&inPeriod(x.paidDate||x.date,bounds));const linked=new Set(paidExpenses.map(x=>x.linkedRepairId).filter(Boolean).map(String));const legacyRepairs=(d.repairs||[]).filter(r=>(!carId||same(r.carId,carId))&&r.status!=='cancelled'&&r.paymentStatus==='paid'&&!linked.has(String(r.id))&&!r.linkedExpenseId&&inPeriod(r.paidDate||r.completedDate||r.date,bounds));return{paidExpenses,legacyRepairs}}FP.FinanceExpenses=Object.freeze({rows});window.financialExpenseRows=rows;console.info('FleetPilot 20 finance expenses ready')})();